
#include "app_main.h"
#include "app_anchor_autocalibrate.h"
#include "app_anchor_run.h"

void app_main() {
    
    while(1) {
        // delay(1000);
        // app_anchor_autocalibrate();
        app_anchor_run();
    }


}